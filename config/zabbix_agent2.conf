# This file is managed by Ansible (community.zabbix role)
# Actual file will be generated by running configure_servers.yml playbook
# and copied here automatically via the post_task "Copy agent config to /opt/repo/config/"
#
# Template ukazuje klíčové direktivy které Ansible nastaví:

# PidFile=/run/zabbix/zabbix_agent2.pid
# LogFile=/var/log/zabbix/zabbix_agent2.log
# LogFileSize=10
# Server=192.168.56.10
# ServerActive=192.168.56.10
# Hostname=bastion-ansible
# HostMetadata=linux ubuntu bastion student
# ListenPort=10050
# Include=/etc/zabbix/zabbix_agent2.d/*.conf
# ControlSocket=/tmp/agent.sock
#
# Po spuštění playbooku bude tento soubor nahrazen skutečnou konfigurací.
